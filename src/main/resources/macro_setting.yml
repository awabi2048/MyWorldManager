# MyWorldが作成されたときに呼び出し。ワールド作成処理が終了したあと実行される
on_world_created:
#  - "say CREATE %owner%, %world_uuid%, %world_name%, %template_name%"
  -
on_world_removed:
  -

# プレイヤーがMyWorldへワープする際に呼び出し
on_world_warp:
#  - "/say WARP %player%, %world_uuid%"
  -

on_member_added:
  - "say ADD %player% %world_uuid%"

on_member_removed:
  - "say REMOVE %player% %world_uuid%"
